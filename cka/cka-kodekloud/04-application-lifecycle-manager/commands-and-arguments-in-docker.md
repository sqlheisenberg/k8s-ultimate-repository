```bash
docker run ubuntu

docker ps 

docker ps -a

#by appending command to docker command you will overide command specified in docker image
docker run ubutnu [COMMAND]

docker run ubuntu sleep 5

#specify command in Dockerfile
CMD ["command", "param1"]
```
